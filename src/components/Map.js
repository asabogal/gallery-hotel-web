import React from 'react';
import styled from 'styled-components';
import {GoogleMap} from 'react-google-maps';

const Map = () => {
  return (
    <div>
      <GoogleMaap/>
    </div>
  );
};

export default Map;